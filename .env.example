# Local development only. Must include the /api suffix.
# Deployed builds ignore this and select PROD/STAGING by hostname — see
# src/_shared/apiService/apiConstants.ts.
VITE_BACKEND_STAGING_BASE_URL=http://localhost:8080/api
